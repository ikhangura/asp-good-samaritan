@model IEnumerable<GoodSamaritan.Models.LookupTables.IncidentModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Incident)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Incident)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IncidentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IncidentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IncidentId })
        </td>
    </tr>
}

</table>
