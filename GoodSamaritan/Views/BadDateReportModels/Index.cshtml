@model IEnumerable<GoodSamaritan.Models.LookupTables.BadDateReportModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BadDateReport)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BadDateReport)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BadDateReportId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BadDateReportId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BadDateReportId })
        </td>
    </tr>
}

</table>
