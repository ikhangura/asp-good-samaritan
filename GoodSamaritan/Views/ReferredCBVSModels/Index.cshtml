@model IEnumerable<GoodSamaritan.Models.LookupTables.ReferredCBVSModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReferredCBVS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReferredCBVS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferralCBVSID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferralCBVSID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferralCBVSID })
        </td>
    </tr>
}

</table>
