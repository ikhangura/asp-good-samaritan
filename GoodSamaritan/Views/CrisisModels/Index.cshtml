@model IEnumerable<GoodSamaritan.Models.LookupTables.CrisisModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Crisis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Crisis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CrisisId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CrisisId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CrisisId })
        </td>
    </tr>
}

</table>
