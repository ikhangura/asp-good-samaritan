@model IEnumerable<GoodSamaritan.Models.LookupTables.VictimOfIncidentModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VictimOfIncident)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VictimOfIncident)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VictimOFIncidentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VictimOFIncidentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VictimOFIncidentId })
        </td>
    </tr>
}

</table>
