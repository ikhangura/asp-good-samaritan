@model IEnumerable<GoodSamaritan.Models.LookupTables.RiskStatusModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RiskStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RiskStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RiskStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RiskStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RiskStatusId })
        </td>
    </tr>
}

</table>
