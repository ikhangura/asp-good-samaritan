@model IEnumerable<GoodSamaritan.Models.LookupTables.MultiplePerpetratorsModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MultiplePerpetrators)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MultiplePerpetrators)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MultiplePerpetratorsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MultiplePerpetratorsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MultiplePerpetratorsID })
        </td>
    </tr>
}

</table>
