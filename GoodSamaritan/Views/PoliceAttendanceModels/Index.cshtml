@model IEnumerable<GoodSamaritan.Models.LookupTables.PoliceAttendanceModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PolicAttendance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PolicAttendance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PoliceAttendanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PoliceAttendanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PoliceAttendanceId })
        </td>
    </tr>
}

</table>
