@model IEnumerable<GoodSamaritan.Models.LookupTables.ThirdPartyReportModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ThirdPartyReport)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ThirdPartyReport)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ThirdPartyReportID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ThirdPartyReportID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ThirdPartyReportID })
        </td>
    </tr>
}

</table>
