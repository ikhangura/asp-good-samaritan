@model IEnumerable<GoodSamaritan.Models.LookupTables.StatusOfFileModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusOfFile)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusOfFile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusOfFileId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusOfFileId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusOfFileId })
        </td>
    </tr>
}

</table>
