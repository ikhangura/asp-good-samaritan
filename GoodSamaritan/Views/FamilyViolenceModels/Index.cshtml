@model IEnumerable<GoodSamaritan.Models.LookupTables.FamilyViolenceModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FamilyViolenceFile)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FamilyViolenceFile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FamilyVolenceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FamilyVolenceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FamilyVolenceId })
        </td>
    </tr>
}

</table>
