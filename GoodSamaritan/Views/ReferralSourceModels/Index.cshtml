@model IEnumerable<GoodSamaritan.Models.LookupTables.ReferralSourceModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReferralSource)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReferralSource)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferralSourceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferralSourceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferralSourceId })
        </td>
    </tr>
}

</table>
