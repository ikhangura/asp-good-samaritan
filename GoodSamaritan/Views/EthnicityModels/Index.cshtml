@model IEnumerable<GoodSamaritan.Models.LookupTables.EthnicityModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ethnicity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ethnicity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EthnicityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EthnicityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EthnicityId })
        </td>
    </tr>
}

</table>
