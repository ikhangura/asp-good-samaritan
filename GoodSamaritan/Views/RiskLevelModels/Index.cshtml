@model IEnumerable<GoodSamaritan.Models.LookupTables.RiskLevelModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RiskLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RiskLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RiskLevelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RiskLevelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RiskLevelId })
        </td>
    </tr>
}

</table>
