@model IEnumerable<GoodSamaritan.Models.LookupTables.CityOfResidenceModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CityOfResidence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CityOfResidence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CityOfResidenceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CityOfResidenceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CityOfResidenceId })
        </td>
    </tr>
}

</table>
