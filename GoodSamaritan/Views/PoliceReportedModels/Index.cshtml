@model IEnumerable<GoodSamaritan.Models.LookupTables.PoliceReportedModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PolicReported)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PolicReported)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PoliceReportedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PoliceReportedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PoliceReportedId })
        </td>
    </tr>
}

</table>
