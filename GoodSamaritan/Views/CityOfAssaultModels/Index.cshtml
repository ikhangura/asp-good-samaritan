@model IEnumerable<GoodSamaritan.Models.LookupTables.CityOfAssaultModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CityOfAssault)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CityOfAssault)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CityOfAssaultId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CityOfAssaultId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CityOfAssaultId })
        </td>
    </tr>
}

</table>
